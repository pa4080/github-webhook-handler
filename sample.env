# Server Configuration
PORT=3000
WEBHOOK_SECRET=your_webhook_secret_key_here
USE_SSH=true

# Test Webhook Configuration
WEBHOOK_URL="http://localhost:3000/webhook"

# SSH Configuration for Private Repositories
SSH_PRIVATE_KEY_PATH=/path/to/your/ssh/private/key
SSH_KEY_PASSPHRASE=optional_passphrase_if_your_key_has_one

# Note: Repository-specific configurations should be placed in the repos/[repository-name]/config.json file
# See app-repos.config.json for an example configuration template
